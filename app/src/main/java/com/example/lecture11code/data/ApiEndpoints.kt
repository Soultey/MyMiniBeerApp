
package com.example.lecture11code.data

// Enum defining API endpoints with their corresponding URLs
enum class ApiEndpoints(val url: String) {
    BASE_URL("https://api.openbrewerydb.org/v1/breweries/random") // Define the base URL for the API endpoint
}
